1. Идентификатор сессии. В таблице postgres, поля:
    login, time_created, time_expire, valid:bool (невалидным может стать в т. ч. из-за логаута)
    (или уж не хранить невалидные?)
    У этого подхода есть недостаток, каждый раз лазить в БД.
2.